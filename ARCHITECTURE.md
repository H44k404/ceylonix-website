# 🏗️ Deployment Architecture

## Local Development Setup

```
┌─────────────────────────────────────────────────────────────┐
│                    Your Computer (Local)                     │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  VS Code / Editor                                    │    │
│  │  ├── src/ (React)                                    │    │
│  │  ├── ceylonix-backend/                              │    │
│  │  └── .env (NOT committed)                           │    │
│  └─────────────────────────────────────────────────────┘    │
│                           ▲                                   │
│                           │                                   │
│         ┌────────────────┼────────────────┐                 │
│         │                │                │                 │
│    npm start         npm run dev    npm install             │
│  (port 3000)       (port 5000)                              │
│         │                │                │                 │
│         ▼                ▼                ▼                 │
│    Frontend         Backend          Dependencies           │
│   React Dev         Express            node_modules         │
│                                                               │
│  http://localhost:3000  ◄────►  http://localhost:5000       │
│         │                              │                     │
│         └──────── Local Testing ───────┘                     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## Production Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        GitHub (Free)                         │
│                                                               │
│  Repository: ceylonix-website                               │
│  ├── Frontend code (src/)                                   │
│  ├── Backend code (ceylonix-backend/)                       │
│  ├── Configuration files                                    │
│  └── .env files ❌ (NOT stored)                             │
│                                                               │
└─────────────────────────────────────────────────────────────┘
         ▲                              ▲
         │ Push code                    │ Connect & Deploy
         │                              │
    ┌────┴──────────────────────────────┴────┐
    │                                         │
    ▼                                         ▼
┌──────────────────┐            ┌──────────────────────┐
│  Vercel (Free)   │            │  Render (Free)       │
│                  │            │                      │
│  Frontend        │            │  Backend             │
│  - React App     │            │  - Express Server    │
│  - Build: npm    │            │  - Build: npm        │
│  - URL: https:// │            │  - Start: node       │
│    yourapp.      │            │  - URL: https://     │
│    vercel.app    │            │    yourapi.onrender  │
│                  │            │    .com              │
│  Env Vars:       │            │                      │
│  - API_BASE_URL  │            │  Env Vars:           │
│    = backend URL │            │  - EMAIL_USER        │
│                  │            │  - EMAIL_PASSWORD    │
│                  │            │  - FRONTEND_URL      │
│                  │            │                      │
└──────────────────┘            └──────────────────────┘
    Deployed ✅                      Deployed ✅
    Public URL                       Public URL
         │                              │
         └──────────┬───────────────────┘
                    │
                    │ CORS Enabled
                    │ Communication
                    │
         ┌──────────┴──────────┐
         │                     │
         ▼                     ▼
    Browser Client      API Requests
    (Visits Website)    (Form submissions)
        │                     │
        └─────────┬───────────┘
                  │
            User Interaction
            (Contact/Booking)
```

---

## Data Flow During Form Submission

```
User fills booking form
         │
         ▼
┌──────────────────────┐
│ Frontend Validation  │  ◄── React component validates
│ (BookingForm.js)     │       • Email format
│                      │       • Required fields
│                      │       • Phone number
└──────────────────────┘
         │
    ✅ Valid? No → Show error alert
         │ Yes
         ▼
┌──────────────────────┐
│ API Call to Backend  │  ◄── FETCH POST
│ (api.js)             │      to /api/booking
│                      │
└──────────────────────┘
         │
         │ HTTPS Request
         │ With CORS headers
         ▼
┌──────────────────────┐
│ Backend Validation   │  ◄── Express route validates
│ (booking.js route)   │       • Email format
│                      │       • Phone validation
│                      │       • Required fields
└──────────────────────┘
         │
    ✅ Valid? No → Return error array
         │ Yes
         ▼
┌──────────────────────┐
│ Send Email           │  ◄── Nodemailer sends
│ (shared.js)          │       via Gmail SMTP
└──────────────────────┘
         │
    ✅ Email sent?
         │ Yes
         ▼
┌──────────────────────┐
│ Save to Database     │  ◄── JSON file write
│ (data/bookings.json) │       (or MongoDB later)
└──────────────────────┘
         │
         ▼
┌──────────────────────┐
│ Return Success JSON  │  ◄── {success: true}
└──────────────────────┘
         │
         │ HTTPS Response
         ▼
┌──────────────────────┐
│ Frontend receives    │  ◄── api.js handles response
│ response             │       error.errors array
└──────────────────────┘
         │
         ▼
┌──────────────────────┐
│ Show Success Alert   │  ◄── Alert component
│ OR Error List        │       displays result
└──────────────────────┘
         │
         ▼
   User sees confirmation
   on screen & receives
   email notification
```

---

## Environment Variables Flow

```
┌────────────────────────────────────┐
│  Development (.env local)          │
│                                    │
│  EMAIL_USER=your@gmail.com         │
│  EMAIL_PASSWORD=apppassword        │
│  PORT=5000                         │
│  FRONTEND_URL=http://localhost:3000│
│                                    │
└────────────────────────────────────┘
          │
          ├─── Never Committed to Git ───┐
          │                               │
          └─────────────────────────────┬─┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
                    ▼                   ▼                   ▼
          ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
          │ Production .env  │ │ Render (Backend) │ │ Vercel (Frontend)│
          │ (Not shared)     │ │                  │ │                  │
          │                  │ │ Environment Vars │ │ Environment Vars │
          │ EMAIL_USER=...   │ │ ├─ EMAIL_USER    │ │ ├─ API_BASE_URL  │
          │ EMAIL_PASSWORD=..│ │ ├─ EMAIL_PASSWORD│ │ │   =backend URL  │
          │ PORT=...         │ │ ├─ EMAIL_RECEIPT │ │ └─ Frontend code  │
          │ FRONTEND_URL=... │ │ ├─ FRONTEND_URL  │ │    uses env var  │
          │                  │ │ └─ PORT          │ │                  │
          └──────────────────┘ └──────────────────┘ └──────────────────┘
                  │                    │                    │
                  │         ┌──────────┴──────────┐        │
                  │         │                     │        │
                  │    ✅ Secure               ✅ Secure   │
                  │    Encrypted              Encrypted   │
                  │    Server-side only       Server-side │
                  │    Never visible          only        │
                  │    in source code         Never visible
                  │                           in source
                  └─────────────────────────────────────┘
                                │
                         User Never Sees
                      Sensitive Information
```

---

## Deployment Checklist Flow

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Prepare Local Project                                    │
│    ✅ .env configured                                       │
│    ✅ Git initialized                                       │
│    ✅ Code committed                                        │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Create GitHub Repository                                │
│    ✅ New repo on github.com                               │
│    ✅ Get repository URL                                   │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Push Code to GitHub                                     │
│    ✅ git remote add origin <repo-url>                     │
│    ✅ git push -u origin main                              │
└─────────────────────────────────────────────────────────────┘
         │
         ├──────────────────────────┬──────────────────────────┐
         ▼                          ▼                          ▼
┌──────────────────┐        ┌──────────────────┐      ┌──────────────────┐
│ 4A. Deploy       │        │ 4B. Deploy       │      │ 4C. Configure    │
│ Backend          │        │ Frontend         │      │ Communications   │
│                  │        │                  │      │                  │
│ Render.com       │        │ Vercel.com       │      │ Update .env in   │
│ ├─ Sign up       │        │ ├─ Sign up       │      │ Render with      │
│ ├─ Connect repo  │        │ ├─ Connect repo  │      │ Frontend URL     │
│ ├─ Select branch │        │ ├─ Select branch │      │                  │
│ ├─ Set root dir  │        │ ├─ Auto-detect   │      │ Render health    │
│ ├─ Add env vars  │        │ ├─ Add env vars  │      │ endpoint         │
│ └─ Deploy ✅     │        │ └─ Deploy ✅     │      │ Check CORS ✅    │
│                  │        │                  │      │                  │
│ Backend URL ✅   │        │ Frontend URL ✅  │      │                  │
└──────────────────┘        └──────────────────┘      └──────────────────┘
         │                          │                          │
         └──────────────┬───────────┴──────────────────────────┘
                        │
                        ▼
         ┌──────────────────────────────────────┐
         │ 5. Test Everything                  │
         │                                      │
         │ ✅ Frontend loads at Vercel URL     │
         │ ✅ Contact form works               │
         │ ✅ Booking form works               │
         │ ✅ Emails send correctly            │
         │ ✅ No CORS errors                   │
         │ ✅ API health endpoint responds     │
         └──────────────────────────────────────┘
                        │
                        ▼
         ┌──────────────────────────────────────┐
         │ 🎉 LIVE & DEPLOYED!                 │
         │                                      │
         │ Website: vercel.app                 │
         │ API: onrender.com                   │
         │ Repo: github.com                    │
         │                                      │
         │ Cost: $0 ✅                         │
         └──────────────────────────────────────┘
```

---

## Key Points

✅ **Separation of Concerns**
- Frontend (Vercel) handles UI
- Backend (Render) handles data
- GitHub stores code

✅ **Security**
- Sensitive data in `.env` files
- Never committed to git
- Encrypted on hosting platforms
- CORS prevents unauthorized access

✅ **Scalability**
- Each service scales independently
- Free tier can handle moderate traffic
- Easy to upgrade when needed

✅ **Cost**
- Vercel: FREE tier includes
- Render: FREE tier includes
- GitHub: FREE tier includes
- **Total: $0**

---

See [QUICK_DEPLOYMENT_CHECKLIST.md](./QUICK_DEPLOYMENT_CHECKLIST.md) for step-by-step instructions!
